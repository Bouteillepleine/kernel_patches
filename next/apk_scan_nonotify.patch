diff --git a/include/util.h b/include/util.h
index c57485cc..45358571 100644
--- a/include/util.h
+++ b/include/util.h
@@ -4,6 +4,10 @@
 #include "linux/fdtable.h" // IWYU pragma: keep
 #include <linux/version.h>
 #include <linux/syscalls.h>
+#include <linux/namei.h>
+#include <linux/cred.h>
+#include <linux/err.h>
+#include <linux/fs.h>
 
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 11, 0)
 #define ksu_close_fd close_fd
@@ -11,4 +15,24 @@
 #define ksu_close_fd ksys_close
 #endif
 
+static inline struct file *ksu_filp_open_nonotify(const char *path, int flags)
+{
+	struct path p;
+	struct file *f;
+	int ret;
+
+	ret = kern_path(path, 0, &p);
+	if (ret)
+		return ERR_PTR(ret);
+
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 18, 0)
+	f = dentry_open_nonotify(&p, flags, current_cred());
+#else
+	f = dentry_open(&p, flags | __FMODE_NONOTIFY, current_cred());
+#endif
+
+	path_put(&p);
+	return f;
+}
+
 #endif
diff --git a/manager/apk_sign.c b/manager/apk_sign.c
index 1ab29d87..a9b0a716 100644
--- a/manager/apk_sign.c
+++ b/manager/apk_sign.c
@@ -1,3 +1,4 @@
+#include "util.h"
 #include <linux/err.h>
 #include <linux/fs.h>
 #include <linux/gfp.h>
@@ -158,15 +159,12 @@ static __always_inline bool check_v2_signature(char *path,
 	int v2_signing_blocks = 0;
 
 	int i;
-	struct file *fp = filp_open(path, O_RDONLY, 0);
+	struct file *fp = ksu_filp_open_nonotify(path, O_RDONLY | O_NOATIME);
 	if (IS_ERR(fp)) {
 		pr_err("open %s error.\n", path);
 		return false;
 	}
 
-	// disable inotify for this file
-	fp->f_mode |= FMODE_NONOTIFY;
-
 	file_size = generic_file_llseek(fp, 0, SEEK_END);
 	if (file_size < 0)
 		goto clean;
diff --git a/manager/throne_tracker.c b/manager/throne_tracker.c
index e005c70b..2a93691b 100644
--- a/manager/throne_tracker.c
+++ b/manager/throne_tracker.c
@@ -7,6 +7,7 @@
 #include <linux/types.h>
 #include <linux/version.h>
 
+#include "util.h"
 #include "policy/allowlist.h"
 #include "manager/apk_sign.h"
 #include "klog.h" // IWYU pragma: keep
@@ -196,7 +197,7 @@ void search_manager(const char *path, int depth, struct list_head *uid_data)
 			struct file *file;
 
 			if (!stop) {
-				file = filp_open(pos->dirpath, O_RDONLY | O_NOFOLLOW, 0);
+				file = ksu_filp_open_nonotify(pos->dirpath, O_RDONLY | O_NOFOLLOW | O_NOATIME);
 				if (IS_ERR(file)) {
 					pr_err("Failed to open directory: %s, err: %ld\n",
 						pos->dirpath, PTR_ERR(file));
